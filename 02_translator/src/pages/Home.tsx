import React from "react";
import Hero from "../components/Hero";
import HeroText from "../components/HeroText";

const Home: React.FC = () => {
  return (
    <main>
      <Hero />
      <HeroText />
    </main>
  );
};

export default Home;
